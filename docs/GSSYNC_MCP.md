# GSSync MCP — инструкции для Claude

Ты имеешь доступ к серверу **GSSync** — инструменту для синхронизации Google Sheets с локальными файлами (xlsx, json, csv). Используй его, когда пользователь просит что-то скачать из таблицы, загрузить данные обратно или проверить содержимое.

---

## Доступные инструменты

### Конфигурация

**`get_config`** — показать текущие настройки по умолчанию (URL таблицы, путь к файлу, формат).

**`set_config`** — сохранить настройки по умолчанию. Все параметры опциональны.
- `spreadsheet_url` — URL таблицы Google Sheets
- `file_path` — путь к локальному файлу
- `file_format` — формат: `xlsx` (по умолчанию), `json`, `csv`

---

### Просмотр листов

**`list_google_sheets`** — список листов в Google Sheets.
- `spreadsheet_url` *(обязательный)* — полный URL таблицы

**`list_local_sheets`** — список листов в локальном файле.
- `file_path` *(обязательный)* — путь к файлу
- `file_format` — формат файла (по умолчанию `xlsx`)

---

### Синхронизация

**`pull_sheet`** — скачать один лист из Google Sheets в локальный файл.
- `spreadsheet_url` *(обязательный)*
- `sheet_name` *(обязательный)* — название листа
- `file_path` *(обязательный)*
- `file_format` — по умолчанию `xlsx`

**`pull_all`** — скачать все листы из Google Sheets в локальный файл.
- `spreadsheet_url` *(обязательный)*
- `file_path` *(обязательный)*
- `file_format` — по умолчанию `xlsx`

**`push_sheet`** — загрузить один лист из локального файла в Google Sheets.
- `spreadsheet_url` *(обязательный)*
- `sheet_name` *(обязательный)*
- `file_path` *(обязательный)*
- `file_format` — по умолчанию `xlsx`

**`push_all`** — загрузить все листы из локального файла в Google Sheets.
- `spreadsheet_url` *(обязательный)*
- `file_path` *(обязательный)*
- `file_format` — по умолчанию `xlsx`

**Форматирование (для всех pull/push инструментов, включая построчные):**
- `include_formatting` — переносить ли форматирование (шрифт, цвет текста, заливка, выравнивание + перенос, ширина столбцов, высота строк, числовой формат, границы). По умолчанию `true`. Работает только для `xlsx`; для `csv`/`json` форматирование пропускается (значения переносятся как обычно).

---

### Построчная работа

Все 4 инструмента принимают **ровно один** фильтр:

| Параметр | Пример | Смысл |
|----------|--------|-------|
| `row_numbers` | `"5"`, `"2,5,10"`, `"2:10"` | Строки по номеру, 1-based (строка 1 = заголовок) |
| `filter_column` + `filter_value` | `"status"`, `"active"` | Все строки, где значение колонки совпадает |
| `cell_range` | `"A2:D10"` | Диапазон в нотации Google Sheets |

**`get_rows`** — прочитать строки из Google Sheets и вывести таблицей.
- `spreadsheet_url` *(обязательный)*
- `sheet_name` *(обязательный)*
- один из фильтров *(обязательный)*

**`update_rows`** — обновить строки в Google Sheets напрямую.
- `spreadsheet_url`, `sheet_name` *(обязательные)*
- `rows_data` *(обязательный)* — JSON-массив объектов: `[{"колонка": "значение"}]`
  - При `filter_column`: первый объект применяется ко всем совпавшим строкам
  - При `cell_range`: `rows_data` — массив массивов значений ячеек
- один из фильтров *(обязательный)*

**`pull_rows`** — скачать отфильтрованные строки из Google Sheets в локальный файл.
- `spreadsheet_url`, `sheet_name`, `file_path` *(обязательные)*
- `file_format` — по умолчанию `xlsx`
- один из фильтров *(обязательный)*

**`push_rows`** — загрузить отфильтрованные строки из локального файла в Google Sheets.
- `spreadsheet_url`, `sheet_name`, `file_path` *(обязательные)*
- `file_format` — по умолчанию `xlsx`
- один из фильтров *(обязательный)*

---

## Примеры использования

**Скачать всю таблицу:**
> "Скачай таблицу https://docs.google.com/spreadsheets/d/ABC в C:\data\report.xlsx"
→ вызов `pull_all`

**Загрузить один лист:**
> "Загрузи лист 'Бюджет' из C:\data\report.xlsx в таблицу"
→ вызов `push_sheet`

**Проверить, что в таблице:**
> "Какие листы есть в таблице https://...?"
→ вызов `list_google_sheets`

**Настроить дефолты один раз:**
> "Запомни: моя таблица https://..., файл C:\data\report.xlsx"
→ вызов `set_config`, затем можно использовать инструменты без явных параметров

**Прочитать конкретную строку:**
> "Покажи строку 5 в листе 'Бюджет' из таблицы https://..."
→ вызов `get_rows` с `row_numbers="5"`

**Обновить все строки с определённым статусом:**
> "Поставь статус 'done' всем строкам где status=active в листе 'Задачи'"
→ вызов `update_rows` с `filter_column="status"`, `filter_value="active"`, `rows_data='[{"status":"done"}]'`

**Скачать только нужные строки в файл:**
> "Скачай строки 1:10 из листа 'Данные' в data.xlsx"
→ вызов `pull_rows` с `row_numbers="1:10"`

---

## Важно

- Аутентификация через OAuth2. При первом запросе откроется браузер для входа в Google — это нормально.
- Токен сохраняется в `~/.gssync/token.json` и обновляется автоматически.
- Формулы сохраняются при синхронизации (не вычисленные значения).
